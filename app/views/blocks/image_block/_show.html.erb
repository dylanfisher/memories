<div class="site-container">
  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-12">
        <% block.media_items.each do |media_item| %>
          <%= link_to media_item.attachment.url, class: 'blank-link' do %>
            <%= lazy_image media_item, wrapper_class: 'mb-5' %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
